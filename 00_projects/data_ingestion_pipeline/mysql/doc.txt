Reset table, eg.:
    > delete from panelists;
    > alter table panelists auto_increment = 1;

DB Disk Usage:
    > sudo du -sh /var/lib/docker/volumes/mysql_mysql_data/_data

Run:
1. docker compose up
2. Producer:
    > cd producer
    > uvicorn app.main:app --host 0.0.0.0 --port 8084
3. Consumer:
    > cd consumer
    > python -m app.main